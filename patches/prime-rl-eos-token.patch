diff --git a/packages/prime-rl-configs/src/prime_rl/configs/trainer.py b/packages/prime-rl-configs/src/prime_rl/configs/trainer.py
index 8f790c2..da35b1f 100644
--- a/packages/prime-rl-configs/src/prime_rl/configs/trainer.py
+++ b/packages/prime-rl-configs/src/prime_rl/configs/trainer.py
@@ -281,6 +281,9 @@ class TokenizerConfig(BaseConfig):
     chat_template: str | None = None
     """Chat template for the tokenizer. Either a Jinja2 template string or a path to a template file. If None, the tokenizer's default chat template is used."""
 
+    eos_token: str | None = None
+    """Existing vocabulary token to use as EOS for training and exported checkpoints."""
+
 
 class ConstantSchedulerConfig(BaseConfig):
     type: Literal["constant"] = "constant"
diff --git a/src/prime_rl/trainer/model.py b/src/prime_rl/trainer/model.py
index a37aca1..031623b 100644
--- a/src/prime_rl/trainer/model.py
+++ b/src/prime_rl/trainer/model.py
@@ -647,7 +647,7 @@ def get_model(
     return model
 
 
-def setup_tokenizer(config: TokenizerConfig) -> PreTrainedTokenizer:
+def setup_tokenizer(config: TokenizerConfig, model=None) -> PreTrainedTokenizer:
     logger = get_logger()
     tokenizer = AutoTokenizer.from_pretrained(config.name, trust_remote_code=config.trust_remote_code)
     if config.chat_template is not None:
@@ -659,7 +659,17 @@ def setup_tokenizer(config: TokenizerConfig) -> PreTrainedTokenizer:
         else:
             logger.info("Using inline custom chat template")
             tokenizer.chat_template = config.chat_template
+    if config.eos_token is not None:
+        if config.eos_token not in tokenizer.get_vocab():
+            raise ValueError(f"EOS token {config.eos_token!r} is not in the tokenizer vocabulary")
+        tokenizer.eos_token = config.eos_token
     tokenizer.pad_token_id = tokenizer.eos_token_id
+    if model is not None:
+        model.config.eos_token_id = tokenizer.eos_token_id
+        model.config.pad_token_id = tokenizer.pad_token_id
+        if model.generation_config is not None:
+            model.generation_config.eos_token_id = tokenizer.eos_token_id
+            model.generation_config.pad_token_id = tokenizer.pad_token_id
 
     return tokenizer
 
diff --git a/src/prime_rl/trainer/rl/train.py b/src/prime_rl/trainer/rl/train.py
index 5bd015c..f8c3a3f 100644
--- a/src/prime_rl/trainer/rl/train.py
+++ b/src/prime_rl/trainer/rl/train.py
@@ -150,7 +150,7 @@ def train(config: TrainerConfig):
     model = setup_model(config.model, parallel_dims, loading_from_ckpt_later)
 
     logger.info(f"Initializing tokenizer ({config.tokenizer})")
-    tokenizer = setup_tokenizer(config.tokenizer)
+    tokenizer = setup_tokenizer(config.tokenizer, model)
 
     # Set up the loss function for the RL loss type (ce / ref_kl are fixed)
     logger.info(f"Setting up loss function ({config.loss})")
diff --git a/src/prime_rl/trainer/sft/train.py b/src/prime_rl/trainer/sft/train.py
index 2abb73e..aa2b2b8 100644
--- a/src/prime_rl/trainer/sft/train.py
+++ b/src/prime_rl/trainer/sft/train.py
@@ -160,7 +160,7 @@ def train(config: SFTConfig):
         multi_run_manager.scaling_factors[0] = config.model.lora.alpha / config.model.lora.rank
 
     logger.info(f"Initializing tokenizer ({config.tokenizer})")
-    tokenizer = setup_tokenizer(config.tokenizer)
+    tokenizer = setup_tokenizer(config.tokenizer, model)
 
     renderer = None
     if config.renderer is not None:
