# Arm A: reactive agent GRPO over all 374 official MBPP train tasks.
max_steps = 94
seq_len = 4096
output_dir = "outputs/arm_a_rl"

[model]
name = "outputs/arm_a_sft/weights/step_24"

[tokenizer]
chat_template = "configs/chat_template.jinja"

[wandb]
project = "predict-mbpp"
group = "matched-arms"
name = "arm-a-grpo"

[orchestrator]
batch_size = 32
group_size = 8

[orchestrator.algo]
type = "grpo"

[orchestrator.renderer]
name = "default"

[orchestrator.train.sampling]
temperature = 0.8
max_completion_tokens = 512

[[orchestrator.train.env]]
name = "arm-a-train"
taskset = { id = "glyph", data_path = "data/arm_a_train.jsonl", task = { max_trace_tokens = 4096 } }
harness = { id = "glyph", arm = "a", runtime = { type = "prime", image = "python:3.12-slim-bookworm", workdir = "/workspace", network_access = true, cpu = 1.0, memory = 2.0, disk = 2.0, idle_timeout = 600, labels = ["predict", "arm-a", "mbpp", "train"] }, max_tool_calls = 8, tool_timeout = 30 }
pool = { type = "elastic", max_workers = 16, multiplex = 4 }

[[orchestrator.post_batch_filters]]
type = "zero_advantage"
enforce = false

[orchestrator.eval]
interval = 24
num_examples = 90
group_size = 1

[orchestrator.eval.sampling]
temperature = 0.0
max_completion_tokens = 512

[[orchestrator.eval.env]]
name = "arm-a-validation"
taskset = { id = "glyph", data_path = "data/arm_a_validation.jsonl", task = { max_trace_tokens = 4096 } }
harness = { id = "glyph", arm = "a", runtime = { type = "prime", image = "python:3.12-slim-bookworm", workdir = "/workspace", network_access = true, cpu = 1.0, memory = 2.0, disk = 2.0, idle_timeout = 600, labels = ["predict", "arm-a", "mbpp", "validation"] }, max_tool_calls = 8, tool_timeout = 30 }
pool = { type = "elastic", max_workers = 16, multiplex = 4 }

[trainer.loss]
type = "default"
kl_tau = 0.0

[trainer.optim]
lr = 1e-6
weight_decay = 0.01

[ckpt]
interval = 24
keep_last = 2

[inference]
gpu_memory_utilization = 0.85
seed = 42

[deployment]
type = "single_node"
num_train_gpus = 1
num_infer_gpus = 1
gpus_per_node = 2
